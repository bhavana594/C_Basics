//Take two numbers num1 and num2 as input from stdin and print the sum of those two numbers.
#include <stdio.h>

int main()
{
    int num1, num2;
    scanf("%d", &num1);
    scanf("%d", &num2);

    int Sum = num1 + num2;
    printf("%d", Sum);

    return 0;
}
